[package]
name = "astropy-siderust"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = ">=0.25,<0.28", features = ["extension-module"] }
